# 外基线：MLP（完全不看图）—— 用来证明「涨点不是只靠参数」
#
# Linear 也在 from 路由里，所以结构上与前两者完全同构，唯一区别是不吃 edge_index。
nc: null

scales:
  n: [0.33, 0.25, 1024]
  s: [0.33, 0.50, 1024]
  m: [0.67, 0.75, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.25, 512]

backbone:
  - [-1, 1, Linear, [64]] # 0
  - [-1, 1, Linear, [64]] # 1

head:
  - [-1, 1, Classify, [nc]] # 2
